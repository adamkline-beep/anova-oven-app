rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Same shape as the Firestore rules: an account owns its own subtree.
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
